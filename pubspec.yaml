name: xenova_health
description: "LeanLog - Professional weight loss, nutrition, intermittent fasting, and fitness tracking"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # UI
  cupertino_icons: any
  fl_chart: ^1.2.0
  cached_network_image: ^3.4.1
  google_fonts: any

  # State Management
  flutter_riverpod: any
  riverpod_annotation: any

  # Routing
  go_router: any

  # Models & Serialization
  freezed_annotation: any
  json_annotation: any

  # Firebase
  firebase_core: any
  firebase_auth: any
  cloud_firestore: any
  firebase_storage: any
  firebase_messaging: any
  firebase_analytics: any
  firebase_crashlytics: any

  # Auth Providers
  google_sign_in: any
  sign_in_with_apple: any

  # Networking
  dio: any

  # Local Storage
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Image
  image_picker: any
  image_cropper: any

  # Barcode
  mobile_scanner: any

  # Notifications
  flutter_local_notifications: ^22.0.0

  # Connectivity
  connectivity_plus: any

  # Security
  flutter_secure_storage: any

  # Permissions
  permission_handler: any

  # Export
  pdf: ^3.12.0
  excel: any
  csv: ^6.0.0
  share_plus: ^13.1.0

  # AI
  google_generative_ai: ^0.4.7

  # Utils
  path_provider: ^2.1.5
  uuid: any
  intl: any
  flutter_dotenv: ^6.0.1
  timezone: ^0.11.0
  flutter_markdown: ^0.7.7+1
  confetti: ^0.8.0
  flutter_image_compress: ^2.4.0
  package_info_plus: ^10.1.0
  firebase_app_check: ^0.4.4+2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Linting
  flutter_lints: any

  # Code Generation
  build_runner: any
  freezed: any
  json_serializable: any
  riverpod_generator: any
  hive_generator: any

  # Testing
  mocktail: any
  golden_toolkit: any
  flutter_launcher_icons: ^0.14.4
  flutter_native_splash: ^2.4.4

flutter:
  uses-material-design: true
  generate: true

  assets:
    - .env
    - assets/images/
    - assets/icons/
    - assets/data/

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/app_logo.png"

flutter_native_splash:
  color: "#FFFFFF"
  image: assets/images/app_logo.png
  android: true
  ios: true
